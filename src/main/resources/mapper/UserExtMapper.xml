<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.haitaos.finallbbs.mapper.UserExtMapper">
    <resultMap id="BaseResultMap" type="com.haitaos.finallbbs.model.User">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Sat Jan 18 17:28:57 CST 2020.
        -->
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="account_id" jdbcType="VARCHAR" property="accountId" />
        <result column="qq_account_id" jdbcType="VARCHAR" property="qqAccountId" />
        <result column="baidu_account_id" jdbcType="VARCHAR" property="baiduAccountId" />
        <result column="weibo_account_id" jdbcType="VARCHAR" property="weiboAccountId" />
        <result column="name" jdbcType="VARCHAR" property="name" />
        <result column="token" jdbcType="CHAR" property="token" />
        <result column="gmt_create" jdbcType="BIGINT" property="gmtCreate" />
        <result column="gmt_modified" jdbcType="BIGINT" property="gmtModified" />
        <result column="avatar_url" jdbcType="VARCHAR" property="avatarUrl" />
        <result column="email" jdbcType="VARCHAR" property="email" />
        <result column="phone" jdbcType="VARCHAR" property="phone" />
        <result column="password" jdbcType="VARCHAR" property="password" />
    </resultMap>

    <select id="selectLatestLoginUser" parameterType="com.haitaos.finallbbs.dto.UserQueryDTO"
            resultMap="BaseResultMap">
        select * from user

        order by gmt_modified desc

        limit #{offset},#{size}
    </select>

    <select id="count" parameterType="com.haitaos.finallbbs.dto.UserQueryDTO"
            resultType="java.lang.Integer">
        select count(*) from user
        <where>
            1=1
            <if test="id != null and id != 0">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="phone != null and phone != ''">
                and phone = #{phone}
            </if>

            <if test="email != null and email != ''">
                and email = #{email}
            </if>
        </where>
    </select>


    <select id="countBySearch" parameterType="com.haitaos.finallbbs.dto.UserQueryDTO"
            resultType="java.lang.Integer">
        select count(*) from user
        <where>

            <if test="search != null and search != ''">
                and name regexp #{search}
            </if>

            <!--       regexp是使用正则表达式     -->
            <if test="start != null and start != ''">
                and gmt_create >= #{start}
            </if>
            <if test="end != null and end != ''">
                and #{end} >= gmt_create
            </if>
        </where>
    </select>

    <select id="selectBySearch" parameterType="com.haitaos.finallbbs.dto.UserQueryDTO"
            resultMap="BaseResultMap">
        select * from user
        <where>

            <if test="search != null and search != ''">
                and  name regexp #{search}
            </if>

<!--       regexp是使用正则表达式     -->
            <if test="start != null and start != ''">
                and gmt_create >= #{start}
            </if>
            <if test="end != null and end != ''">
                and #{end} >= gmt_create
            </if>

        </where>
            order by gmt_modified desc limit #{offset},#{size}
    </select>




</mapper>